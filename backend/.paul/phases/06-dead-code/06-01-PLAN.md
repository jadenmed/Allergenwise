---
phase: 06-dead-code
plan: 01
plan_type: execute
wave: 1
depends_on: []
files_modified:
  - "lib/search.ts (delete)"
  - "lib/supabase/client.ts (delete)"
  - "lib/types/auth.ts (delete)"
  - "components/allergen/AllergenBadge.tsx (delete)"
  - "components/allergen/index.ts (delete)"
  - "components/course/index.ts (delete)"
  - "components/shell/index.ts (delete)"
  - "components/directory/index.ts (delete)"
  - "components/ui/avatar.tsx (delete)"
  - "components/ui/dropdown-menu.tsx (delete)"
  - "components/ui/separator.tsx (delete)"
  - "components/ui/switch.tsx (delete)"
  - "components/ui/tooltip.tsx (delete)"
  - "lib/mux.ts (trim 2 exports)"
  - "lib/directory/geocode.ts (demote 1 export)"
  - "lib/types/db.ts (delete 7 dead interfaces)"
  - "next.config.js (remove no-op webpack block)"
  - "package.json (remove 8 deps, add server-only)"
  - "pnpm-lock.yaml (regenerated)"
autonomous: false
description: "B5+B6+B7 dead-code sweep: delete unreferenced files/exports/deps, evidence-gated per deletion"
type: Plan
about: allergenwise
---

<objective>
## Goal
Remove provably-unreferenced files, exports, and dependencies (audit items B5, B6, B7). Every deletion is gated on grep evidence of zero consumers on the current integration tip (aa80033, post-04/09 merges). Add the missing `server-only` dependency (audit-flagged latent bug: 40 files import it; it resolves only via Next.js's bundled compiled copy, not from node_modules).

## Purpose
Smaller install graph, less dead surface for future phases (coming-soon gate, OAuth, Figma hook-up) to trip over. Zero behavior change.

## Out of scope (evidence-based rulings)
- `signMuxPlaybackUrl` + `mux` client in lib/mux.ts — production signing path, tests cover it, step-14 video wiring incoming.
- `@mux/mux-node` / `@mux/mux-player-react` deps — video work mid-integration (audit UNCERTAIN, PROMPT_PLAN step 14).
- curriculum.ts type exports — form loadCurriculum()'s public return-type chain; internally referenced, not dead.
- db.ts enum aliases (RestaurantStatus, ReviewStatus, CertRevocationReason, ExamDifficulty, …) + partner/deletion/warning row types — internally referenced by row types / Database interface.
- `computeCheckChar` (B7) — already internal-only in lib/learner/cert-code.ts on current tip; no action.
- `api.mapbox.com` images.remotePattern — geocoding REST API still in use; only the no-op webpack block goes.

## Output
- Deletions per table above; full verify tally vs baseline (typecheck 0, build 0, vitest 724/4/1 with the 4 known failures unchanged)
- `.paul/phases/06-dead-code/06-01-SUMMARY.md`
</objective>

<acceptance_criteria>

## AC-1: Every deletion evidence-gated
Each deleted file/export/dep has a recorded grep proof of zero non-doc consumers at aa80033.

## AC-2: Behavior-preserving
No route, component, or lib consumed by live code is touched except to remove dead exports. No test files modified.

## AC-3: Verify tally matches baseline
typecheck 0 errors, build success, vitest --no-file-parallelism = 724 pass / 4 fail / 1 skip, failures identical to baseline (reconcile-cert-states ×3 + cert-code entropy).

## AC-4: Dep graph consistent
pnpm install clean after package.json edit; no phantom imports left (grep proves no source references removed deps).
</acceptance_criteria>
